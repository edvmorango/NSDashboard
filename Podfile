# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NSDashboard' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NSDashboard

  pod 'Firebase/Core'
  pod 'Firebase/Database'
  pod 'RxSwift',    '~> 3.0'
  pod 'RxCocoa',    '~> 3.0'

  target 'NSDashboardTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'NSDashboardUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
